<ul>
  {{#each model as |repository|}}
    {{!-- the left side is inside the component 
          and the right side is the outside world --}}
    {{github-repo repo = repository}} 
  {{/each}}
</ul>

{{outlet}}
